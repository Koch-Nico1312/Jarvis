# JARVIS UI Shell

This folder contains the React/Vite UI shell used by the desktop assistant.

## Running the code

Run `npm install` to install the dependencies.

Run `npm run dev` to start the development server.
  
