
# JARVIS UI Shell

This folder contains the React/Vite UI shell we can use to rebuild the JARVIS assistant interface.

The original design source was imported from Figma and kept as a component library-style bundle.

  ## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
  
